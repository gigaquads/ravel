from pybiz.api.web.falcon import FalconServiceFunctionRegistry
from pybiz.api.web.falcon.middleware.json import JsonBodyMiddleware


class {{ project.context.project.name|camel }}WebService(FalconServiceFunctionRegistry):

    @property
    def middleware(self):
        return [
            JsonBodyMiddleware(),
        ]

