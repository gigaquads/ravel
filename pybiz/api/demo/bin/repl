#!/usr/bin/env python3

from os.path import join, dirname, realpath
from demo.api import repl


manifest_filepath = join(
  dirname(realpath(__file__)), '..', 'manifest.yml'
)
print(manifest_filepath)
repl.bootstrap(manifest_filepath)
repl.start()
